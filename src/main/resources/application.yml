spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/employee_db
    username: postgres
    password: Naga@1818

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  h2:
    console:
      enabled: true
      path: /h2-console

  security:
    user:
      name: admin
      password: admin

server:
  port: 8080

jwt:
  secret: verySecretKeyChangeThisToLongRandomString
  expirationMs: 3600000  # 1 hour in milliseconds
